# IAM Roles for Services

- Some AWS services need to perform actions on your behalf
- To do so, we assign permissions to AWS services using **IAM Roles**
- If we create an EC2 instance (virtual server), we give it a Role so it can call other AWS services

## Common Roles

- EC2 Instance Roles
- Lambda Function Roles
- Roles for CloudFormation
